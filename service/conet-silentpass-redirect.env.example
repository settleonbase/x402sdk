# Copy to /etc/default/conet-silentpass-redirect on API hosts (e.g. 38.102.126.50).
# Port 14000 — NOT 4000 (reserved for Prysm beacon gRPC on L1 validator nodes).
SILENTPASS_REDIRECT_PORT=14000
# CANCUN_RPC=http://127.0.0.1:8888
