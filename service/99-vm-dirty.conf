# Reduce dirty page cache to prevent IO spikes
# Default (40/20) causes large flushes; lower values = smoother writes
# Postgres / execution client (geth, reth) more stable
vm.dirty_ratio=10
vm.dirty_background_ratio=5
